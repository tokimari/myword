<h1>Editing word</h1>
<article>
  <%= render 'form' %>
</article>
<footer class="cf">
  <% if user_signed_in? %>
    <div class="r">
      <%= link_to 'Show', @word %> |
      <%= link_to 'Destroy', @word, method: :delete, data: { confirm: 'Are you sure?' } %>
    </div>
  <% end %>
  <div class="l">
    <%= link_to 'Back', words_path %>
  </div>
</footer>
